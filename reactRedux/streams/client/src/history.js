/**
 * history is a package that is installed alongside react-browser-dom, its kind of like a state for the history of the app, i.e. createBrowserHistory creates a new history object that we maintain.
 */
import { createBrowserHistory } from 'history'; 

export default createBrowserHistory();